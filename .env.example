# Expo Mobile App Environment Example
# Copy to .env (never commit real secrets)

# Azure AD / Entra ID Configuration
AZURE_CLIENT_ID=
AZURE_TENANT_ID=
# For mobile: gammonmmexpo://auth
# For Vercel web deployment: https://your-vercel-domain.vercel.app/auth
AZURE_REDIRECT_URI=gammonmmexpo://auth
AZURE_AUTHORITY=https://login.microsoftonline.com/{tenant-id}

# API Configuration
API_BASE_URL=https://dev.material-api.example.com
API_SCOPE=api://{client-id}/erp.mm.api

# Environment tag (DEV|UAT|PRE|PROD)
APP_ENV=DEV

# Feature flags (comma separated or boolean)
FEATURE_APPROVALS=true
FEATURE_ATTACHMENTS=true
FEATURE_OFFLINE_STAGING=true

# Update channel (expo-updates)
EXPO_UPDATE_CHANNEL=development

# Public analytics key (if any)
ANALYTICS_PUBLIC_KEY=

# Log batching endpoint relative path
LOG_ENDPOINT=/log/events

# Permissions cache TTL minutes
PERMISSIONS_TTL_MINUTES=10

# Performance thresholds (ms)
PERF_P90_COLD_START=3200
PERF_P90_SEARCH=1500
PERF_P90_SCAN_SUBMIT=1100

# ===== VERCEL DEPLOYMENT NOTES =====
# When deploying to Vercel:
# 1. Update AZURE_REDIRECT_URI to: https://your-vercel-domain.vercel.app/auth
# 2. Add all these environment variables in Vercel project settings
# 3. Update your Azure AD app registration with the new web redirect URI
# 4. Ensure EXPO_PUBLIC_* variables are set for client-side access
